package swati4star.createpdf.util;

import org.junit.Test;

public class DialogUtilsTest {

    @Test
    public void showFilesInfoDialog() {
        /*
        DialogUtils dialogUtils = Mockito.mock(DialogUtils.class);
        int dialogId = 20;
        Activity activity = Mockito.spy(Activity.class);
        Mockito.doCallRealMethod().when(dialogUtils).showFilesInfoDialog(activity, dialogId);
        dialogUtils.showFilesInfoDialog(activity, dialogId);
        Mockito.verify(dialogUtils, Mockito.times(1))
                .showFilesInfoDialog(activity, dialogId);*/
    }

    @Test
    public void showFilesInfoDialog2() {
        /*
        DialogUtils dialogUtils = Mockito.mock(DialogUtils.class);
        int dialogId = 22;
        Activity activity = Mockito.mock(Activity.class);
        Mockito.doCallRealMethod().when(dialogUtils).showFilesInfoDialog(activity, dialogId);
        dialogUtils.showFilesInfoDialog(activity, dialogId);
        Mockito.verify(dialogUtils, Mockito.times(1))
                .showFilesInfoDialog(activity, dialogId);*/
    }
}