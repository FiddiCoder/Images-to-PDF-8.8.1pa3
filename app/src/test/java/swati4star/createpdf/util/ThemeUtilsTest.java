package swati4star.createpdf.util;


import android.content.Context;

import org.junit.Test;
import org.mockito.Mock;

public class ThemeUtilsTest {

    @Mock
    Context context;

    @Test
    public void setThemeApp() {
        /*
        MockitoAnnotations.initMocks(this);
        PreferenceManager preferenceManager = Mockito.mock(PreferenceManager.class);
        ThemeUtils themeUtils = Mockito.mock(ThemeUtils.class);
        SharedPreferences sharedPreferences = Mockito.mock(SharedPreferences.class);
        Mockito.when(context.getSharedPreferences(anyString(), anyInt())).thenReturn(sharedPreferences);
        Mockito.when(sharedPreferences.getString(Constants.DEFAULT_THEME_TEXT,
                Constants.DEFAULT_THEME)).thenReturn(THEME_WHITE);
        Mockito.doCallRealMethod().when(themeUtils);
        ThemeUtils.setThemeApp(context);
        Mockito.verify(themeUtils, Mockito.times(1));
        ThemeUtils.setThemeApp(context);

         */
    }
}