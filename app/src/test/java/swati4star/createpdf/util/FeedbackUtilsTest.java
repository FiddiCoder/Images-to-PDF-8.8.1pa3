package swati4star.createpdf.util;

import org.junit.Test;

public class FeedbackUtilsTest {



    @Test
    public void openWebPage() {
        /*
        String url = "http://www.google.com";
        FeedbackUtils feedbackUtils = Mockito.mock(FeedbackUtils.class);
        Mockito.doCallRealMethod().when(feedbackUtils).openWebPage(url);
        feedbackUtils.openWebPage(url);
        Mockito.verify(feedbackUtils, Mockito.times(1)).openWebPage(url);

         */
    }
}